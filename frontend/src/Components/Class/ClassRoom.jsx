import React, { useState, useEffect } from "react";
import Video from "twilio-video";

const ClassRoom = ({ roomName, token, handleLogout }) => {
  const [room, setRoom] = useState(null);
  const [participants, setParticipants] = useState([]);
};

export default Room;
